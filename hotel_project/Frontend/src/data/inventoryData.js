/**
 * Each item has: id, name, unit, current stock, reorder threshold.
 */


/** Unit options for inventory items */
export const UNIT_OPTIONS = ['kg', 'g', 'l', 'ml', 'pcs', 'bunch', 'packet', 'box'];
